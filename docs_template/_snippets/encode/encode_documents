python DOCUMENTS=DOCUMENTS, ENCODED_DOCUMENTS=encoded_documents
# documents are saved as a list of dictionaries
documents = <<DOCUMENTS>>

# Encode the `<<FIELD>>` field in a list of documents
<<ENCODED_DOCUMENTS>> = model.encode_documents([<<FIELD>>], documents)